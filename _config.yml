# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Gervis Bermudez | Blog
email: gervisbermudez@outlook.com
description: 'I develop creative solutions to complex problems, from technical tests to custom CMSs, turning code and design into working products.'
baseurl: "" # the subpath of your site, e.g. /blog
url: "" # the base hostname & protocol for your site, e.g. http://example.com
paginate: 6 # Número de publicaciones por página
paginate_path: "/blog/page:num/" # Ruta de las páginas de paginación
twitter_username: gervisbermudez
github_username: gervisbermudez
default_banner: /public/uploads/2024/03/luca-bravo-XJXWbfSo2f0-unsplash-2.jpg
timezone: America/Argentina/Buenos_Aires
permalink: /blog/:categories/:year/:month/:day/:title
default_author: Gervis Bermudez
# Build settings
# theme: minima
plugins:
  - jekyll-feed
  - jekyll-paginate # Paginación de publicaciones
  - jekyll-seo-tag # SEO
  - jekyll-sitemap # Sitemap
# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
exclude:
  - graphify-out
defaults:
  - scope:
      path: "public"
    values:
      sitemap: false
